# 单向平台(One-Way Platform)设计

日期: 2026-08-08
状态: 已批准(设计阶段),待写实现计划

## 背景

角色移动控制(跑动/跳跃/朝向翻转)已经完成并验证([2026-08-07 框架设计](2026-08-07-elemagic-framework-design.md) 路线图第 1 项)。原路线图第 2 项是"敌人 + 基础战斗闭环",但开发优先级临时调整:先完善地形交互和角色作为横版游戏应有的核心移动能力,再回头做敌人/战斗。

## 本轮范围与后续排期

这次调整涉及 3 个角色能力(冲刺/闪避、墙跳/贴壁滑落、二段跳)和 3 个地形交互(单向平台、危险地形、拾取物),不可能一次做完。商定的增量顺序:

1. **单向平台**(本轮设计并实现)—— 最基础的地形机制,后续能力都会在这类平台上验证,先把地形基础打好。
2. 二段跳 —— 三个角色能力里最简单,直接扩展现有跳跃逻辑。
3. 冲刺/闪避 —— 相对独立,需要设计冷却与输入绑定。
4. 墙跳/贴壁滑落 —— 最复杂,需要贴墙状态检测,且依赖关卡里有合适的墙体。
5. 危险地形(占位块)—— 伤害机制,用占位红色方块先跑通,美术资源到位后直接换贴图。
6. 拾取物/金币 —— 相对独立,可插入前面任意顺序之间。

每一项仍然各自走独立的"头脑风暴 → 计划 → 实现"循环,本文档只覆盖第 1 项。

## 设计:`AOneWayPlatform`

> **2026-08-08 实现修正**:下面这版设计在 PIE 里实测跑不通,原因和最终修法记在实现计划(`docs/superpowers/plans/2026-08-08-oneway-platform.md`)的 "Post-Implementation Notes" 里,概括如下——(1) `CollisionBox` 单独一个组件时,只要它对 Pawn 是 Block 就**永远不会**触发重叠事件(UE 里两个组件的碰撞响应取双向 min,Block 对 Block 的 min 还是 Block),所以额外加了一个纯检测用的 `DetectionBox`(Overlap-only,比 CollisionBox 上下多探出一截);(2) 忽略碰撞必须用**组件级**的 `IgnoreComponentWhenMoving(CollisionBox, ...)`,不能用下面写的 Actor 级 `MoveIgnoreActors`——Actor 级忽略会让角色对 `DetectionBox` 也一起失明,一旦开始穿透就再也检测不到,永远撤不回忽略状态,角色会直接掉穿到底。最终代码结构以实现计划为准,这里的描述仅供理解设计意图。

### 组件结构

- `UBoxComponent CollisionBox` —— 碰撞盒,默认对 Pawn 是 Block,同时开启重叠事件(`GenerateOverlapEvents = true`)。
- `UPaperSpriteComponent Sprite` —— 视觉表现,先用占位方块精灵;以后可以直接换成 `Tilemap_Platform` 里的贴图,C++ 逻辑不用改。
- 纯 C++ 基类,不挂任何具体美术。具体每种单向平台(木板、石台等)都是 BP 子类,只换 `Sprite` 的贴图。这跟项目里计划中的 `AElementPickupActor`、test_25d 的 `MyEffectActor` 是同一套"一个 C++ 基类、多个内容 BP 子类"模式——**以后所有单向平台都从这个 Actor 继承**,不新开平行的实现。

### 穿透 / 支撑判定逻辑

平台持续追踪与自身重叠的 `ACharacter`(玩家,以后也适用敌人)。每次检测比较:
- 角色脚底 Z(胶囊体底部)与平台顶面 Z 的相对位置
- 角色当前垂直速度方向

规则:
- 角色脚底低于平台顶面 **且** 正在向上运动(跳跃穿过程中)→ 把 `CollisionBox` 这个组件加进角色胶囊体的忽略列表,角色物理意义上"穿过去"。
- 角色脚底已经到达/高于平台顶面(准备落地或已站在上面)→ 把 `CollisionBox` 从该角色的忽略列表移除,恢复正常碰撞阻挡,角色被正常支撑,可以像走在实体地面上一样活动。
- 角色**已经在穿透中**时,只看位置(脚底是否真正越过顶面),不看速度方向——避免跳跃最高点恰好卡在 `CollisionBox` 内部、速度过零那一帧被误判成"该恢复阻挡"从而被引擎的解穿插逻辑弹飞。

这个"是否应该穿透"的判断本身是一个不依赖 `UWorld` 的纯函数:

```
bool ShouldPassThroughPlatform(float CharacterFeetZ, float PlatformTopZ, float CharacterVelocityZ, bool bWasPassingThrough)
```

输入角色脚底 Z、平台顶面 Z、角色垂直速度、是否已经在穿透中,输出是否应该穿透。跟移动控制那次的 `ACharacterBase::SelectFlipbookForState` 是同一套路——把可判定的核心逻辑抽成纯函数,单独写自动化测试覆盖边界情况;真正的物理穿透效果(忽略是否生效)通过 PIE 手动验证,原因跟之前一致:本项目没有走 UWorld 的自动化测试基础设施,能测的部分测,不能测的部分靠明确的手动验证清单。

### 验收方式

- **自动化测试**(纯函数 `ShouldPassThroughPlatform` 的边界情况):
  - 脚底低于平台顶面 + 正在上升 → 应穿透
  - 脚底低于平台顶面 + 正在下降 → 不应穿透(避免从平台下方贴着往下掉时被吸进平台内部)
  - 脚底已到达/高于平台顶面(不论速度方向)→ 不应穿透(正常支撑站立/行走)
- **PIE 手动验证**:在 `level1` 放一块 `BP_OneWayPlatform_Placeholder`(占位方块贴图),确认:
  1. 从平台下方起跳能穿过上升
  2. 到达平台上方后能稳定站立、正常行走
  3. 从平台边缘走出/主动往下能正常掉落(不会卡住)
  4. 反复跳上跳下多次没有卡死或抖动

## 未决问题(留到后续增量再定)

- 平台是否需要支持"单向 + 移动"的组合(移动平台在路线图里单独排开,本轮不实现移动)。
- 多个角色(玩家+敌人)同时与同一平台交互时的边界情况,留到敌人系统真正接入时再验证。
