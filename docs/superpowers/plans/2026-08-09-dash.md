# 冲刺实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 C 键触发的面朝方向冲刺（Dash），0.15s / 400 单位 / 纯水平位移 / 无敌帧 / 地面 CD 空中清零 / 空中限 1 次 / 落地重置

**Architecture:** GAS GameplayAbility（GA_Dash），通过 InputConfig 路由输入（零 C++ 输入改动）。冲刺以 Timer 驱动每 Tick 写 Velocity.X = Direction * 2667。地面 CD 走 GAS Cooldown GE + 进入空中时 RemoveActiveEffectsWithGrantedTags 清零。空中次数限制走 State_DashedInAir tag 的 ActivationBlockedTags 检查。动画通过 SelectFlipbookForState 新增 Dash 最高优先级。

**Tech Stack:** C++（GAS、CharacterMovementComponent、PaperFlipbook）、Blueprint（Cooldown GE、InputConfig、IMC）

## Global Constraints

- 代码约定：C++ 机制 + Blueprint/DataAsset 内容，同 test_25d 模式
- 输入走 InputConfig→BindAbilityActions 路径，不再新增 PlayerController 直接绑定
- 冲刺动画占位符：DashFlipbook 在 BP 中设，未设时不崩溃（SelectFlipbookForState 退回 Idle）
- 自动化测试：CDO 上测 AbilityTags/ActivationBlockedTags/速度常量等，不测 CDO 上的 CMC 值（见 [[feedback_testing_postinit]]）
- PIE 手动验证全部冲刺行为

---

### Task 1: 新增冲刺相关 GameplayTag

**Files:**
- Modify: `Source/Elemagic/Public/ElemagicGameplayTags.h`
- Modify: `Source/Elemagic/Private/ElemagicGameplayTags.cpp`

**Interfaces:**
- Produces: `ElemagicGameplayTags::Ability_Dash` — 输入绑定→能力激活
- Produces: `ElemagicGameplayTags::State_Dashing` — 冲刺进行中，阻挡重叠激活 + 动画驱动
- Produces: `ElemagicGameplayTags::State_DashedInAir` — 空中已冲过，阻挡再次空中冲刺
- Produces: `ElemagicGameplayTags::State_Invulnerable` — 冲刺无敌帧，伤害系统检查此 tag
- Produces: `ElemagicGameplayTags::Cooldown_Dash` — 冲刺冷却 tag，Cooldown GE 授予，地面 CD 清零时按此移除

- [ ] **Step 1: 在头文件中声明新 tag**

在 `ElemagicGameplayTags.h` 的命名空间内，`State_Dead` 之后追加：

```cpp
	ELEMAGIC_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(Ability_Dash);

	ELEMAGIC_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(State_Dashing);
	ELEMAGIC_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(State_DashedInAir);
	ELEMAGIC_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(State_Invulnerable);
	ELEMAGIC_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(Cooldown_Dash);
```

- [ ] **Step 2: 在 cpp 中定义新 tag**

在 `ElemagicGameplayTags.cpp` 的 `UE_DEFINE_GAMEPLAY_TAG` 块追加：

```cpp
	UE_DEFINE_GAMEPLAY_TAG(Ability_Dash, "Ability.Dash");

	UE_DEFINE_GAMEPLAY_TAG(State_Dashing, "State.Dashing");
	UE_DEFINE_GAMEPLAY_TAG(State_DashedInAir, "State.DashedInAir");
	UE_DEFINE_GAMEPLAY_TAG(State_Invulnerable, "State.Invulnerable");
	UE_DEFINE_GAMEPLAY_TAG(Cooldown_Dash, "Cooldown.Dash");
```

- [ ] **Step 3: 编译验证**

```powershell
& "C:\Program Files\Epic Games\UE_5.8\Engine\Build\BatchFiles\Build.bat" ElemagicEditor Win64 Development -Project="C:\Users\greedy\Documents\Unreal Projects\Elemagic\Elemagic.uproject" -WaitMutex -FromMsBuild
```

预期: 编译成功。

- [ ] **Step 4: 提交**

```bash
git add Source/Elemagic/Public/ElemagicGameplayTags.h Source/Elemagic/Private/ElemagicGameplayTags.cpp
git commit -m "feat: add Dash gameplay tags (Ability, State, Cooldown, Invulnerable)"
```

---

### Task 2: 更新 CharacterBase — IsFacingRight + DashFlipbook 动画

**Files:**
- Modify: `Source/Elemagic/Public/CharacterBase.h`
- Modify: `Source/Elemagic/Private/CharacterBase.cpp`

**Interfaces:**
- Consumes: `ElemagicGameplayTags::State_Dashing`（Task 1）
- Produces: `ACharacterBase::IsFacingRight()` — public getter，GA_Dash 用
- Produces: `ACharacterBase::DashFlipbook` — 冲刺序列帧属性
- Produces: `SelectFlipbookForState` 新签名 — 新增 bIsDashing 和 DashFlipbook 参数，Dash 最高优先级
- Produces: `UpdateAnimation` — 检查 `State_Dashing` tag 传冲刺状态

- [ ] **Step 1: 头文件修改**

在 `SelectFlipbookForState` 声明处（~Line 39），改为带 Dash 参数的签名：

```cpp
	// 优先级:冲刺(有 DashFlipbook 才用) > 下落(有 JumpFlipbook 才用) > 移动(有 RunFlipbook 才用) > 待机。
	static UPaperFlipbook* SelectFlipbookForState(bool bIsDashing, bool bIsFalling, bool bIsMoving,
		UPaperFlipbook* IdleFlipbook, UPaperFlipbook* RunFlipbook, UPaperFlipbook* JumpFlipbook, UPaperFlipbook* DashFlipbook);
```

在 JumpFlipbook 属性之后新增：

```cpp
	// 冲刺专用序列帧,目前没有专门美术素材,先留空;SelectFlipbookForState 会在未赋值时退回 IdleFlipbook。
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Elemagic|Animation")
	TObjectPtr<UPaperFlipbook> DashFlipbook;
```

在 `IsDead()` 之后新增 public getter：

```cpp
	UFUNCTION(BlueprintPure, Category = "Elemagic|Character")
	bool IsFacingRight() const { return bFacingRight; }
```

- [ ] **Step 2: SelectFlipbookForState 新实现**

```cpp
UPaperFlipbook* ACharacterBase::SelectFlipbookForState(bool bIsDashing, bool bIsFalling, bool bIsMoving,
	UPaperFlipbook* IdleFlipbook, UPaperFlipbook* RunFlipbook, UPaperFlipbook* JumpFlipbook, UPaperFlipbook* DashFlipbook)
{
	if (bIsDashing && DashFlipbook)
	{
		return DashFlipbook;
	}
	if (bIsFalling && JumpFlipbook)
	{
		return JumpFlipbook;
	}
	if (bIsMoving && RunFlipbook)
	{
		return RunFlipbook;
	}
	return IdleFlipbook;
}
```

- [ ] **Step 3: UpdateAnimation 传入冲刺状态**

```cpp
void ACharacterBase::UpdateAnimation()
{
	UPaperFlipbookComponent* SpriteComp = GetSprite();
	if (!SpriteComp)
	{
		return;
	}

	const bool bIsDashing = AbilitySystemComponent && AbilitySystemComponent->HasMatchingGameplayTag(ElemagicGameplayTags::State_Dashing);
	const bool bIsFalling = GetCharacterMovement() && GetCharacterMovement()->IsFalling();
	const bool bIsMoving = !FMath::IsNearlyZero(GetVelocity().X);
	UPaperFlipbook* DesiredFlipbook = SelectFlipbookForState(bIsDashing, bIsFalling, bIsMoving,
		IdleFlipbook, RunFlipbook, JumpFlipbook, DashFlipbook);

	if (DesiredFlipbook && SpriteComp->GetFlipbook() != DesiredFlipbook)
	{
		SpriteComp->SetFlipbook(DesiredFlipbook);
	}
}
```

- [ ] **Step 4: 编译验证**

```powershell
& "C:\Program Files\Epic Games\UE_5.8\Engine\Build\BatchFiles\Build.bat" ElemagicEditor Win64 Development -Project="C:\Users\greedy\Documents\Unreal Projects\Elemagic\Elemagic.uproject" -WaitMutex -FromMsBuild
```

预期: 编译成功。

- [ ] **Step 5: 提交**

```bash
git add Source/Elemagic/Public/CharacterBase.h Source/Elemagic/Private/CharacterBase.cpp
git commit -m "feat: add IsFacingRight getter, DashFlipbook, and dash animation priority"
```

---

### Task 3: 创建 GA_Dash 冲刺能力类

**Files:**
- Create: `Source/Elemagic/Public/GA_Dash.h`
- Create: `Source/Elemagic/Private/GA_Dash.cpp`

**Interfaces:**
- Consumes: `ElemagicGameplayTags`（Task 1）, `ACharacterBase::IsFacingRight()`（Task 2）
- Produces: `UGA_Dash` — 继承 `UElemagicGameplayAbility`
  - 构造：`AbilityTags` → `Ability.Dash`; `ActivationOwnedTags` → `State.Dashing`; `ActivationBlockedTags` → `State.Dashing`, `State_DashedInAir`; `CooldownTags` → `Cooldown.Dash`
  - `ActivateAbility()` — CommitAbility → 计算方向 → 加 `State_Invulnerable` → 空中则加 `State_DashedInAir` → 启动 DashTick/DashEnd Timer
  - `EndAbility()` — 清除 Timer → 移除 `State_Invulnerable`
  - `DashTick()` — 写 `MoveComp->Velocity.X = Sign * 2667`
  - `GetDashDirectionSign()` — 通过 `IsFacingRight()` 返回 ±1
  - `IsInAir()` — 检查 CMC::IsFalling
  - 成员：`DashSpeed` (2667), `DashDuration` (0.15)

- [ ] **Step 1: 编写 GA_Dash.h**

```cpp
// GA_Dash.h
#pragma once

#include "CoreMinimal.h"
#include "ElemagicGameplayAbility.h"
#include "GA_Dash.generated.h"

/**
 * 冲刺能力:面朝方向 0.15s / 400 单位水平位移,带无敌帧。
 * 地面有 GAS 冷却(空中清零),空中每滞空限 1 次(State_DashedInAir tag 阻挡)。
 * 输入通过 InputConfig 的 Ability.Dash -> ASC::TryActivateAbilitiesByTag 路由。
 */
UCLASS()
class ELEMAGIC_API UGA_Dash : public UElemagicGameplayAbility
{
	GENERATED_BODY()

public:
	UGA_Dash();

	virtual void ActivateAbility(const FGameplayAbilitySpecHandle Handle,
		const FGameplayAbilityActorInfo* ActorInfo,
		const FGameplayAbilityActivationInfo ActivationInfo,
		const FGameplayEventData* TriggerEventData) override;

	virtual void EndAbility(const FGameplayAbilitySpecHandle Handle,
		const FGameplayAbilityActorInfo* ActorInfo,
		const FGameplayAbilityActivationInfo ActivationInfo,
		bool bReplicateCancel, bool bEndedByCancel) override;

	// 冲刺速度(单位/秒):2667 = 400 / 0.15
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Dash")
	float DashSpeed = 2667.f;

	// 冲刺持续时长(秒)
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Dash")
	float DashDuration = 0.15f;

private:
	void DashTick();
	int32 GetDashDirectionSign() const;
	bool IsInAir() const;

	FTimerHandle DashTickTimer;
	FTimerHandle DashEndTimer;
};
```

- [ ] **Step 2: 编写 GA_Dash.cpp**

```cpp
// GA_Dash.cpp
#include "GA_Dash.h"
#include "CharacterBase.h"
#include "ElemagicGameplayTags.h"
#include "GameFramework/Character.h"
#include "GameFramework/CharacterMovementComponent.h"
#include "AbilitySystemComponent.h"

UGA_Dash::UGA_Dash()
{
	AbilityTags.AddTag(ElemagicGameplayTags::Ability_Dash);

	ActivationOwnedTags.AddTag(ElemagicGameplayTags::State_Dashing);

	ActivationBlockedTags.AddTag(ElemagicGameplayTags::State_Dashing);
	ActivationBlockedTags.AddTag(ElemagicGameplayTags::State_DashedInAir);

	CooldownTags.AddTag(ElemagicGameplayTags::Cooldown_Dash);

	// InstancingPolicy / NetExecutionPolicy 由父类 UElemagicGameplayAbility 统一设置
}

void UGA_Dash::ActivateAbility(const FGameplayAbilitySpecHandle Handle,
	const FGameplayAbilityActorInfo* ActorInfo,
	const FGameplayAbilityActivationInfo ActivationInfo,
	const FGameplayEventData* TriggerEventData)
{
	if (!CommitAbility(Handle, ActorInfo, ActivationInfo))
	{
		EndAbility(Handle, ActorInfo, ActivationInfo, true, true);
		return;
	}

	UAbilitySystemComponent* ASC = ActorInfo->AbilitySystemComponent.Get();
	ACharacter* Character = Cast<ACharacter>(ActorInfo->AvatarActor.Get());
	if (!ASC || !Character)
	{
		EndAbility(Handle, ActorInfo, ActivationInfo, true, true);
		return;
	}

	ASC->AddLooseGameplayTag(ElemagicGameplayTags::State_Invulnerable);

	if (IsInAir())
	{
		ASC->AddLooseGameplayTag(ElemagicGameplayTags::State_DashedInAir);
	}

	UWorld* World = GetWorld();
	if (World)
	{
		World->GetTimerManager().SetTimer(DashTickTimer, this, &UGA_Dash::DashTick, 0.001f, true);
		World->GetTimerManager().SetTimer(DashEndTimer,
			[this, Handle, ActorInfo, ActivationInfo]()
			{
				EndAbility(Handle, ActorInfo, ActivationInfo, true, false);
			},
			DashDuration, false);
	}

	// 第一帧立即推一次速度,不等 Timer 首 tick 延迟
	DashTick();
}

void UGA_Dash::EndAbility(const FGameplayAbilitySpecHandle Handle,
	const FGameplayAbilityActorInfo* ActorInfo,
	const FGameplayAbilityActivationInfo ActivationInfo,
	bool bReplicateCancel, bool bEndedByCancel)
{
	if (UWorld* World = GetWorld())
	{
		World->GetTimerManager().ClearTimer(DashTickTimer);
		World->GetTimerManager().ClearTimer(DashEndTimer);
	}

	if (UAbilitySystemComponent* ASC = ActorInfo->AbilitySystemComponent.Get())
	{
		ASC->RemoveLooseGameplayTag(ElemagicGameplayTags::State_Invulnerable);
	}

	Super::EndAbility(Handle, ActorInfo, ActivationInfo, bReplicateCancel, bEndedByCancel);
}

void UGA_Dash::DashTick()
{
	ACharacter* Character = Cast<ACharacter>(GetAvatarActorFromActorInfo());
	if (!Character) return;

	UCharacterMovementComponent* MoveComp = Character->GetCharacterMovement();
	if (!MoveComp) return;

	MoveComp->Velocity.X = GetDashDirectionSign() * DashSpeed;
}

int32 UGA_Dash::GetDashDirectionSign() const
{
	const ACharacterBase* CharBase = Cast<ACharacterBase>(GetAvatarActorFromActorInfo());
	if (CharBase)
	{
		return CharBase->IsFacingRight() ? 1 : -1;
	}
	return 1;
}

bool UGA_Dash::IsInAir() const
{
	const ACharacter* Character = Cast<ACharacter>(GetAvatarActorFromActorInfo());
	if (!Character) return false;

	const UCharacterMovementComponent* MoveComp = Character->GetCharacterMovement();
	return MoveComp && MoveComp->IsFalling();
}
```

- [ ] **Step 3: 编译验证**

```powershell
& "C:\Program Files\Epic Games\UE_5.8\Engine\Build\BatchFiles\Build.bat" ElemagicEditor Win64 Development -Project="C:\Users\greedy\Documents\Unreal Projects\Elemagic\Elemagic.uproject" -WaitMutex -FromMsBuild
```

预期: 编译成功。

- [ ] **Step 4: 提交**

```bash
git add Source/Elemagic/Public/GA_Dash.h Source/Elemagic/Private/GA_Dash.cpp
git commit -m "feat: add GA_Dash ability with dash tick, invulnerability, and air dash limit"
```

---

### Task 4: PlayerCharacter — 空中 CD 清零 + 落地重置

**Files:**
- Modify: `Source/Elemagic/Public/PlayerCharacter.h`
- Modify: `Source/Elemagic/Private/PlayerCharacter.cpp`

**Interfaces:**
- Consumes: `ElemagicGameplayTags::Cooldown_Dash`, `ElemagicGameplayTags::State_DashedInAir`（Task 1）
- Produces: `APlayerCharacter::Landed()` override — 落地移除 `State_DashedInAir`
- Produces: `APlayerCharacter::OnMovementModeChanged()` override — 进入空中移除 Cooldown GE

- [ ] **Step 1: 头文件新增 override 声明**

在 `PlayerCharacter.h` 的 `PostInitializeComponents` 声明后：

```cpp
	virtual void Landed(const FHitResult& Hit) override;
	virtual void OnMovementModeChanged(EMovementMode PrevMovementMode, uint8 PreviousCustomMode) override;
```

- [ ] **Step 2: cpp 实现**

```cpp
void APlayerCharacter::Landed(const FHitResult& Hit)
{
	Super::Landed(Hit);

	if (AbilitySystemComponent)
	{
		AbilitySystemComponent->RemoveLooseGameplayTag(ElemagicGameplayTags::State_DashedInAir);
	}
}

void APlayerCharacter::OnMovementModeChanged(EMovementMode PrevMovementMode, uint8 PreviousCustomMode)
{
	Super::OnMovementModeChanged(PrevMovementMode, PreviousCustomMode);

	if (!AbilitySystemComponent)
	{
		return;
	}

	if (GetCharacterMovement() && GetCharacterMovement()->MovementMode == MOVE_Falling)
	{
		FGameplayTagContainer CooldownTag;
		CooldownTag.AddTag(ElemagicGameplayTags::Cooldown_Dash);
		AbilitySystemComponent->RemoveActiveEffectsWithGrantedTags(CooldownTag);
	}
}
```

- [ ] **Step 3: 编译验证**

```powershell
& "C:\Program Files\Epic Games\UE_5.8\Engine\Build\BatchFiles\Build.bat" ElemagicEditor Win64 Development -Project="C:\Users\greedy\Documents\Unreal Projects\Elemagic\Elemagic.uproject" -WaitMutex -FromMsBuild
```

预期: 编译成功。

- [ ] **Step 4: 提交**

```bash
git add Source/Elemagic/Public/PlayerCharacter.h Source/Elemagic/Private/PlayerCharacter.cpp
git commit -m "feat: clear dash cooldown on falling and reset air dash on landing"
```

---

### Task 5: 自动化测试

**Files:**
- Create: `Source/Elemagic/Private/Tests/PlayerCharacterDashTests.cpp`

**Interfaces:**
- Consumes: `UGA_Dash`（Task 3）, `ElemagicGameplayTags`（Task 1）

- [ ] **Step 1: 编写测试**

```cpp
// PlayerCharacterDashTests.cpp
#if WITH_AUTOMATION_TESTS

#include "Misc/AutomationTest.h"
#include "GA_Dash.h"
#include "ElemagicGameplayTags.h"

IMPLEMENT_SIMPLE_AUTOMATION_TEST(FPlayerCharacterDashTest, "Elemagic.PlayerCharacter.Dash",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)

bool FPlayerCharacterDashTest::RunTest(const FString& Parameters)
{
	const UGA_Dash* CDO = GetDefault<UGA_Dash>();
	if (!TestNotNull(TEXT("GA_Dash CDO exists"), CDO))
	{
		return false;
	}

	TestTrue(TEXT("GA_Dash has Ability.Dash in AbilityTags"),
		CDO->AbilityTags.HasTagExact(ElemagicGameplayTags::Ability_Dash));

	TestTrue(TEXT("GA_Dash grants State.Dashing"),
		CDO->ActivationOwnedTags.HasTagExact(ElemagicGameplayTags::State_Dashing));

	TestTrue(TEXT("GA_Dash blocked by State.Dashing (no overlap)"),
		CDO->ActivationBlockedTags.HasTagExact(ElemagicGameplayTags::State_Dashing));
	TestTrue(TEXT("GA_Dash blocked by State.DashedInAir (air limit)"),
		CDO->ActivationBlockedTags.HasTagExact(ElemagicGameplayTags::State_DashedInAir));

	TestTrue(TEXT("GA_Dash cooldown tag is Cooldown.Dash"),
		CDO->CooldownTags.HasTagExact(ElemagicGameplayTags::Cooldown_Dash));

	TestEqual(TEXT("DashSpeed is 2667 (400 units over 0.15s)"), CDO->DashSpeed, 2667.f);
	TestEqual(TEXT("DashDuration is 0.15s"), CDO->DashDuration, 0.15f);

	TestTrue(TEXT("GA_Dash blocked by State.Dead (from base class)"),
		CDO->ActivationBlockedTags.HasTagExact(ElemagicGameplayTags::State_Dead));

	return true;
}

#endif // WITH_AUTOMATION_TESTS
```

- [ ] **Step 2: 编译并运行测试**

编译:
```powershell
& "C:\Program Files\Epic Games\UE_5.8\Engine\Build\BatchFiles\Build.bat" ElemagicEditor Win64 Development -Project="C:\Users\greedy\Documents\Unreal Projects\Elemagic\Elemagic.uproject" -WaitMutex -FromMsBuild
```

运行:
```powershell
& "C:\Program Files\Epic Games\UE_5.8\Engine\Binaries\Win64\UnrealEditor-Cmd.exe" "C:\Users\greedy\Documents\Unreal Projects\Elemagic\Elemagic.uproject" -ExecCmds="Automation RunTests Elemagic.PlayerCharacter.Dash;quit" -unattended -NoSound -log -ABSLOG="C:\Users\greedy\Documents\Unreal Projects\Elemagic\Saved\Logs\TestOutput_Dash.log"
```

预期: 全部 PASS。

- [ ] **Step 3: 提交**

```bash
git add Source/Elemagic/Private/Tests/PlayerCharacterDashTests.cpp
git commit -m "test: add GA_Dash CDO tag/speed/duration assertions"
```

---

### Task 6: 蓝图配置（UE Editor 手动操作）

**无需代码改动。**

- [ ] **Step 1: 创建 IA_Dash**

`Content/Input/` 下新建 `InputAction` 资源 `IA_Dash`，Value Type = `Digital (bool)`。

- [ ] **Step 2: IMC_Player 中绑定 C 键**

打开 `IMC_Player`，添加 `IA_Dash` → C 键映射。

- [ ] **Step 3: InputConfig 中添加 Tag 映射**

打开 `EleInputConfig`，AbilityInputActions 中添加 `(IA_Dash, Ability.Dash)`。

- [ ] **Step 4: 创建 GE_DashCooldown**

新建 `GameplayEffect` 资源 `GE_DashCooldown`，Duration Policy = `Has Duration`，Duration Magnitude = 0.8。

- [ ] **Step 5: BP_PlayerCharacter 配置**

打开 `BP_PlayerCharacter` → Class Defaults：
- Abilities 数组 → 添加 `GA_Dash`
- `GA_Dash` 的 Cooldown GameplayEffect → 选 `GE_DashCooldown`
- Animation 分类 → 设置 `DashFlipbook`（暂用 IdleFlipbook 占位）

- [ ] **Step 6: 提交蓝图改动**

```bash
git add Content/
git commit -m "chore: configure dash input, cooldown GE, and BP dash settings"
```

---

### Task 7: PIE 手动验证

**无需代码改动。**

- [ ] **Step 1: 地面冲刺**

PIE，按 C 键。确认面朝方向冲刺约 400 单位，持续约 0.15s。

- [ ] **Step 2: 地面 CD**

地面冲刺后 CD 内再按 C。确认不触发。

- [ ] **Step 3: 空中 CD 清零**

地面冲刺 → 立即起跳 → 空中 C 键。确认可触发空中冲刺。

- [ ] **Step 4: 空中次数限制**

空中冲刺一次后，滞空期间再按 C。确认不触发。

- [ ] **Step 5: 落地重置**

落地后按 C。确认地面冲刺可用、空中次数重置。

- [ ] **Step 6: 连续循环 5 次**

地冲→起跳→空冲→落地...反复 5 次无异常。

- [ ] **Step 7: 冲刺动画**

冲刺期间 DashFlipbook 切换，结束后恢复。

- [ ] **Step 8: 提交关卡改动（如有）**

```bash
git add Content/Level/level1.umap
git commit -m "chore: level1 PIE dash verification"
```
