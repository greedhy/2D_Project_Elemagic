#pragma once

namespace UE::SOMOLMCP
{
	class FSololmcpToolRegistry;

	/** Registers the P2 native C++ foliage authoring completion surface. */
	void RegisterFoliageCompletionTools(FSololmcpToolRegistry& Registry);
}
